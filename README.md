# Store API

A Java Spring Boot learning project. Each commit introduces one concept with working code.

## Running

```bash
./mvnw spring-boot:run   # dev mode with hot reload
./mvnw test              # run tests
./mvnw package           # build jar
```

API docs available at `http://localhost:8080/swagger-ui.html` when running.

